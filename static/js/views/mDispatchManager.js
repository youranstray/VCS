//控制到矩阵
define([
	'backbone'
	], 
	function (Backbone) {
	App.Views.MDispatchManager = Backbone.View.extend({
		events: {

		},
		initialize: function (options) {

		},
		render: function () {
			
		}
	});
	return App.Views.MDispatchManager;
});
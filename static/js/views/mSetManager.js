//矩阵配置
define([
	'backbone'
	], 
	function (Backbone) {
	App.Views.MSetManager = Backbone.View.extend({
		events: {

		},
		initialize: function (options) {

		},
		render: function () {
			
		}
	});
	return App.Views.MSetManager;
});
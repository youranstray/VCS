define(['backbone'], function (Backbone) {
	App.Routers.Router = Backbone.Router.extend({
		routes:{
			'*page': 'showPage' 
		},
		initialize: function () {
			var router = this;
			//router.navigator();
		},
		showPage: function () {

		},

	});
	return App.Routers.Router;
});